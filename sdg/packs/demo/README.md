# Demo Pack

This pack exists to prove the commons contract during Phase 0.

It generates deterministic addition rows with hidden answer state, verifies the
targets programmatically, and publishes stripped train/eval parquet exports.
